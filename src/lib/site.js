/* [PLACEHOLDER: replace example.com with the real deployed domain — keep it
   in sync with the URLs in index.html, public/sitemap.xml and public/robots.txt] */
export const SITE_URL = 'https://example.com';
